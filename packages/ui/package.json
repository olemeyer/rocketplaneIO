{
  "name": "@rocketplane/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Offenes Design-System von rocketplane — Tokens (Status-Semantik, Signal-Identität, Theming) und später Komponenten.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens": "./src/tokens.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@rocketplane/tsconfig": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^3.2.0"
  }
}
