# Generated by cmd/genmanifests — do not edit by hand.
# Declares set_env's reversibility, risk and target surface; the agent runs
# the audited pipeline and inverse under this contract.
compensation: builtin
params:
- name: name
  required: true
  type: string
- name: value
  type: string
- name: container
  type: string
recipe: set_env
reversibility: full
risk:
  base: reversible
steps:
- kind: mutate
  name: trigger
- kind: observe
  name: observe
- kind: verify
  name: verify
targets:
- Deployment
- StatefulSet
- DaemonSet
title: Set a container env var
