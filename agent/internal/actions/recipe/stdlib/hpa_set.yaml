# Generated by cmd/genmanifests — do not edit by hand.
# Declares hpa_set's reversibility, risk and target surface; the agent runs
# the audited pipeline and inverse under this contract.
compensation: builtin
params:
- name: minReplicas
  type: int
- name: maxReplicas
  type: int
recipe: hpa_set
reversibility: full
risk:
  base: reversible
steps:
- kind: mutate
  name: trigger
- kind: observe
  name: observe
- kind: verify
  name: verify
targets:
- HorizontalPodAutoscaler
title: Set HPA bounds
