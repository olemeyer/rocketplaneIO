# Generated by cmd/genmanifests — do not edit by hand.
# Declares node_untaint's reversibility, risk and target surface; the agent runs
# the audited pipeline and inverse under this contract.
compensation: builtin
params:
- name: key
  required: true
  type: string
recipe: node_untaint
reversibility: full
risk:
  base: reversible
steps:
- kind: mutate
  name: trigger
- kind: observe
  name: observe
- kind: verify
  name: verify
targets:
- Node
title: Remove a node taint
